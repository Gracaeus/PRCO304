using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
[Serializable]

public class placementManager : MonoBehaviour
{
    // Start is called before the first frame update
    public string chosenBox;
    void Start()
    {
        chosenBox = "Default";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
